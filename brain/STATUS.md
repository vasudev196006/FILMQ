# STATUS — Current Development State

> **What this file answers**: "Where are we right now? What's next? What's blocked?"
> **Soft cap**: 80 lines. Overwrite freely as development progresses.

---

## Current State

- **Dev Server**: Live on `http://localhost:5173/` (`@workspace/cinefy`).
- **Official Brand Title**: **FILMQ**
- **Debug Toggle Removed**: Completely removed the debug theme toggle button and state from `Navbar.tsx`.
- **Liquid Glass Refraction Search Bar**: Multi-stage 3D liquid glass refraction shadows & SVG `backdropFilter` live on `SearchPage.tsx`.
- **Movie Card Glare Fix**: Clean, dark high-contrast movie cards without white fog overlays or square glare boxes.
- **Theme Color**: Iconic **Netflix Crimson Red (`#E50914`)**, Obsidian Slate, and Crisp White.
- **Circular Logo Container**: Enclosed `logo.png` inside circular image wrappers (`size-8 rounded-full overflow-hidden`).
- **TMDB Live API**: 100% Live using active `VITE_TMDB_API_KEY`.

## Active Priorities

1. **User Review**: User inspecting clean dark mode app on `http://localhost:5173/`.
